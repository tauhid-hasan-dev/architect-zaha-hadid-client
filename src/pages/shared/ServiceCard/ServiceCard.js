import React from 'react';

const ServiceCard = ({ service }) => {
    const { title, img, price, description } = service;
    return (
        <div>
            <p>this is service card</p>
        </div>
    );
};

export default ServiceCard;